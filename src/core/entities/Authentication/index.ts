import { Authentication } from './Authentication';

export default Authentication;
export type { Authentication as IAuthentication } from './Authentication';
export type { AuthenticationController as IAuthenticationController } from './@types/Authentication.types';
